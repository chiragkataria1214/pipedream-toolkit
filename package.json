{
  "name": "pipedream-toolkit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Laptop-only toolkit for Pipedream workflows: refresh local exports and scaffold new workflows on disk. Pushing back into Pipedream is a manual UI paste — the public REST API can't populate workflow content. Works with Cursor (.cursor/rules) and Claude Code (claude/skills).",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "refresh": "node scripts/refresh-exports.mjs",
    "refresh:dry": "node scripts/refresh-exports.mjs --dry-run",
    "create": "node scripts/create-workflow.mjs",
    "build-tree": "node scripts/build-tree.mjs"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "dotenv": "^16.4.5"
  }
}
